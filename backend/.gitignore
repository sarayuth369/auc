node_modules/
.wrangler/
dist/
.dev.vars
*.log

# Regenerated by `wrangler types`; not hand-maintained, not needed for our
# own tsconfig (we type Env ourselves in config.ts/resolve.ts).
worker-configuration.d.ts
